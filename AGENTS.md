# 杀五行_修复版2 项目规则

继承上级 `AGENTS.md`。本文件只记录本项目差异。

## 范围与修改

- 只修改本项目；`杀五行-修复版` 未经点名不得修改、同步、覆盖或删除。
- 用户未明确“开始”前，只能审计和说明；不得抓取、测试、写入或删除。
- 用户明确“开始”后，只执行指定的项目、站点、URL、期数、方向和文件。
- 修改保持局部、可审计、可回滚；不顺手清理无关改动。
- 回复使用中文并以“老大”开头。

## 统一流水线

所有入口复用同一条正式流水线：稳定 `site_id/rule_id` → 站点规则 → 来源获取 → 专属解析 → 统一校验 → 输出/缓存。

- `sources` 只获取原始 `SourceDocument`；`parsers` 只解析候选和证据；`services` 编排流程；`storage` 负责缓存、锁和原子写入；`reporting` 只格式化结果；`cli` 只调度和选择写入策略。
- 不得在入口、解析器或报告层重复实现网络、方向、冲突或五行判断。
- 普通站点不得跨文档拼接；专属解析失败不得回退通用解析。
- 每个结果必须绑定同一站点、URL/记录 ID、栏目/作者/关键词、权威区块、期号、方向、候选顺序和解析器证据。

## 数据校验

- 五行只能是 `金行`、`木行`、`水行`、`火行`、`土行`。
- `top` 只取目标区块前三条，`bottom` 只取后三条；指定期越界、缺失、来源不明或区块不唯一均失败。
- 同站同一期出现冲突候选必须失败；相同值也必须确认属于同一权威区块。
- 禁止用缓存、相邻期、其他栏目、其他文档、其他站点或广告补值。
- HTTP、API、浏览器、iframe、script、OCR 都是独立来源；不得跨来源拼接或放宽校验。

## 期数、缓存与失败重抓

- 单期实时访问指定期；缓存只在结果定稿后更新最近 10 期。
- 多期逐期处理，但不得更新正式缓存，除非上级规则或用户明确授权。
- 封存站点不抓取、不写入、不更新缓存；恢复或删除必须点名。
- 失败重抓只读取失败 TXT 中的站点，逐站验证；验证成功才更新对应当前期缓存和成功 TXT，并清除对应失败记录；失败记录保留。
- 成功 TXT 必须保留旧内容和值顺序，只追加指定成功站点；重复运行不得重复追加，同站同期期号冲突必须失败。
- 成功、失败文件和缓存均使用锁、临时文件和原子替换；不得生成 `.txt.lock`。
- 失败文件处理期间发生变化时停止写入，保护新增记录。

## 站点规则与验收

- 新增或修复站点必须真实验证来源、专属解析、期数、方向、有效历史和正式判重；规则变更递增 `rule_version`。
- 专属规则应保存脱敏原始夹具、预期期数/五行/方向、边界和失败诱饵。
- 按变更范围运行相关测试、入口启动检查、Python 编译和指定期真实流程；未真实验证不得声称完成。

## GitHub 同步

- `origin`：`https://github.com/xiaobai156/wuxing.git`。
- 本项目修改通过必要验证后，自动提交并推送到 `origin`。
- 只提交本次相关代码、配置、规则和必要文档；保留已有未提交改动，不提交凭据、环境文件、临时文件、锁文件、缓存或编译产物。
