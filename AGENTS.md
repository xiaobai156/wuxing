# 杀五行_修复版2长期架构与操作规范

> 文档状态：完成
>
> 解耦实施状态：进行中
>
> 适用目录：`C:\Users\Administrator\Desktop\每天工具\数据系列\杀五行_修复版2`

## 1. 最高优先级与操作授权

- 本规范只适用于 `杀五行_修复版2`。`杀五行-修复版` 是现有正式版本，除非用户明确指定，否则不得修改、覆盖、同步或删除其中任何文件。
- 回复用户必须使用中文，并以“老大”开头。
- 不能自作主张，必须严格按用户明确说明执行，不能擅自扩大范围、改变方向、增删站点或替用户决定。
- 用户提出新需求时，先确认目标、范围、输入、输出、边界和验收标准；只有重大歧义需要提问，小歧义可以说明合理假设。
- 未收到用户明确的“开始”，不得修改代码、配置、缓存、TXT、BAT 或规则文件。只读检查和方案说明不属于写入授权。
- 删除站点、修正历史缓存、迁移正式入口和清理旧代码必须分别获得对应范围的明确授权，不能从一般“开始”推断扩大权限。
- 修改必须局部、可审计、可回滚。不得顺手重构、格式化或清理与当前任务无关的区域。

## 2. 长期目标与不可妥协原则

本项目采用“单一核心流水线 + 站点专属插件 + 全程证据链 + 事务缓存 + 兼容入口”的长期架构。

抓取精度永远高于速度。任何速度优化都不能放宽以下条件：

- 期号必须是用户手动指定的期号，或当前操作明确允许的期号集合。
- 五行必须来自该期号、该站点、该栏目、该权威正文中的唯一标准结果。
- `top`/`bottom`、顶部/尾部、上/下必须按站点配置执行。
- 标题、作者、栏目、关键词、正文和结果必须属于同一记录、同一文档边界。
- 同站同一期出现多个高可信候选且五行冲突时必须失败，不能自行挑选。
- 不得用缓存、相邻期、其他关键词、其他栏目、推荐内容、广告、导航、上一篇/下一篇、隐藏源码或其他站点数据凑数。
- 无法证明来源正确时必须失败，不能猜测。

## 3. 唯一正式流水线

所有入口必须调用同一条正式流水线：

```text
读取并校验站点配置
→ 根据稳定 site_id/rule_id 解析站点规则
→ 按规则获取带来源信息的原始文档
→ 锁定权威文档、文章ID和目标数据块
→ 运行站点专属解析器
→ 校验指定期号
→ 校验 top/bottom 三条位置窗口
→ 校验关键词、标准五行和正文边界
→ 校验同期冲突、重复五行和跨文档污染
→ 生成统一 ScrapeResult
→ 成功结果才允许进入报告和缓存写入阶段
```

单期、多期、重复检测、失败站验证不得维护第二套抓取、解析、方向判断或冲突判断逻辑。任何入口都不能跳过正式流水线中的校验步骤。

## 4. 最终目录边界

目标目录结构如下：

```text
杀五行_修复版2/
├─ wuxing/
│  ├─ domain/
│  │  ├─ models.py
│  │  ├─ errors.py
│  │  └─ enums.py
│  ├─ config/
│  │  ├─ settings.py
│  │  ├─ site_loader.py
│  │  └─ schema.py
│  ├─ sources/
│  │  ├─ http.py
│  │  ├─ browser.py
│  │  ├─ documents.py
│  │  ├─ article_api.py
│  │  ├─ spa.py
│  │  └─ ocr.py
│  ├─ parsers/
│  │  ├─ common.py
│  │  ├─ registry.py
│  │  ├─ topic_sites.py
│  │  ├─ forum_sites.py
│  │  ├─ article_sites.py
│  │  └─ special_sites.py
│  ├─ validation/
│  │  ├─ authority.py
│  │  ├─ position.py
│  │  ├─ conflict.py
│  │  └─ result.py
│  ├─ services/
│  │  ├─ scrape.py
│  │  ├─ batch.py
│  │  ├─ multi_period.py
│  │  └─ duplicate.py
│  ├─ storage/
│  │  ├─ history_cache.py
│  │  ├─ file_lock.py
│  │  └─ migrations.py
│  ├─ reporting/
│  │  ├─ progress.py
│  │  ├─ success.py
│  │  └─ failure.py
│  └─ registry.py
├─ cli/
│  ├─ single.py
│  ├─ multi.py
│  ├─ duplicate.py
│  └─ failed_validator.py
├─ tests/
│  ├─ unit/
│  ├─ integration/
│  ├─ fixtures/
│  ├─ contract/
│  └─ live/
├─ wuxing_crawler.py
├─ wuxing_multi_period.py
├─ wuxing_duplicate_flexible.py
├─ failed_site_validator.py
├─ sites.json
├─ recent_10_cache.json
└─ AGENTS.md
```

不得为了追求文件数量而过度拆分。普通站点按页面结构归类，真正存在独立正文边界、点击入口、四行补缺、OCR 或特殊冲突规则的站点才实现独立适配逻辑。

## 5. 依赖方向

- `domain` 只包含数据模型、枚举和错误类型，不允许依赖网络、Selenium、文件系统、CLI 或具体站点。
- `sources` 只负责获取原始内容和来源元数据，不允许解析或判断五行。
- `parsers` 只接收 `SourceDocument` 并产生候选，不允许访问网络、缓存、TXT 或命令行。
- `validation` 只执行可重复的纯校验，不允许抓取页面或写文件。
- `services` 负责编排获取、权威边界、解析和校验，不直接实现站点正则。
- `storage` 只负责缓存读取、校验、锁和事务写入，不参与抓取判断。
- `reporting` 只消费统一结果，不得重新解析字符串决定成功或失败。
- `cli` 只解析参数、调用服务并选择写入策略，不允许包含业务规则。
- 兼容入口只能调用 `cli` 或 `services` 的公开接口，不得重新实现旧逻辑。
- 必须增加架构测试，阻止反向依赖、循环依赖和跨层直接调用内部函数。

## 6. 核心数据契约

至少定义以下不可变数据模型：

- `SiteConfig`：`site_id`、名称、URL、方向、`rule_id`、API URL、是否启用及配置版本。
- `ScrapeRequest`：指定期数或明确允许的期数集合、模式、超时、是否显示浏览器、只读/可写策略。
- `SourceDocument`：文档 ID、原始 URL、来源类型、父文档 ID、文章/记录 ID、是否浏览器渲染、原始顺序、正文和内容摘要。
- `Candidate`：期号、标准五行、来源文档 ID、记录 ID、锚点、关键词、正文边界、原始位置、原始文本、`rule_version`。
- `ValidationEvidence`：期号证据、方向窗口、标题/作者/栏目、关键词、五行、冲突和边界检查结果。
- `ScrapeResult`：明确状态、唯一候选、失败代码、中文失败原因、证据链、耗时和重试记录。
- `BatchResult`：按站点保存最终结果，不使用易错的位置元组传递关键状态。

不得再使用无来源信息的普通字符串文档或 `(site, lines, error, documents, elapsed)` 位置元组作为长期接口。

## 7. 站点规则与专属插件

- `sites.json` 中每个正式目录必须有稳定且唯一的 `site_id` 和明确 `rule_id`。
- 站点规则只能在唯一注册表中登记，不能同时散落在多个 URL 集合和 `if site.url == ...` 分支中。
- 每条规则组合获取策略、权威文档选择器、解析器、锚点、关键词、边界和必要的附加校验。
- 站点插件可以增加更严格的限制，不能绕过或放宽全局期号、方向、唯一五行和冲突铁律。
- 新增站点和修复已有站点必须真实访问页面/API/浏览器内容，并根据用户截图确定页面结构后建立专属规则。
- 截图是制定专属解析的结构证据，不只是证明页面存在数据；截图不能代替真实访问。
- 每个新增或修复的专属规则必须保存脱敏后的真实原始内容夹具，并记录预期期号、五行、方向、边界和失败诱饵。
- 已存在的明确特例必须按完整 URL 或稳定 `site_id` 精确迁移。新增特例必须由用户针对具体站点明确授权；“全部添加”不能自动产生特例。
- 规则变更必须递增 `rule_version`，并说明变更原因和受影响站点。

## 8. 原始内容、权威边界与动态接口

- 每份原始内容必须保留来源：页面、脚本、iframe、API、SPA、浏览器 DOM 或 OCR。
- 普通情况下不得把多份文档简单拼接后全局正则扫描。
- 标题和正文必须来自同一文档或同一结构化记录，不能跨文章、跨 iframe、跨 API 对象拼接。
- `/article/admin/`、`/article/manager/` 及其他动态详情页必须从详情 URL 提取唯一记录 ID。
- 聚合接口必须结构化遍历 JSON，并按记录 ID 精确匹配唯一对象；不能按第一篇、期号、作者或关键词单独选择记录。
- ID 唯一匹配后，还必须校验作者、标题、栏目、方向和目标字段。
- 找不到 ID、找到多个同 ID、标题正文不属于同一记录或字段校验失败时一律失败。
- API 404 或有效页面空壳允许按站点规则进入浏览器兜底；兜底后仍执行相同的 ID、边界和关键词检查。
- iframe 和子脚本不能获得比主权威文档更宽松的位置窗口。
- 需要点击标签或入口的站点，必须唯一匹配指定期数、站名和专属关键词后才能点击。

## 9. 抓取、浏览器、重试与 OCR

- HTTP、浏览器、API、SPA 和 OCR 实现为可替换来源适配器，但都返回统一 `SourceDocument`。
- HTTP 默认最多 8 并发，浏览器默认最多 2 并发；调整并发前必须先做精度和稳定性对照测试。
- 重试只能重新执行相同精度的获取和解析流程，不能放宽关键词、期号、方向、边界或冲突规则。
- 超时、TLS、HTTP 状态、页面空壳、入口不存在、浏览器失败和解析失败必须使用不同失败代码。
- 浏览器复用必须隔离站点状态；Cookie、页面、标签页或脚本状态可能污染下一站时必须重建浏览器上下文。
- OCR 不能只信 JavaScript 像素分类。Python 必须二次校验期号、标准五行、原始 OCR 文本、独立识别、分数和候选差值。
- OCR 版式变化、缺行、插行、字体变化、低置信度或结果冲突时必须失败。
- 速度统计记录首次尝试和重试的累计耗时，不能只保留最后一次耗时。

## 10. 指定期号、方向和冲突铁律

- 手动单期必须实时访问页面，缓存不能替代抓取。
- `top`、顶部、上：只允许目标数据块靠前方向的前三条高可信候选。
- `bottom`、尾部、底部、下：只允许目标数据块靠后方向的后三条高可信候选。
- 指定期不在对应三条窗口时直接失败。
- 同站同一期出现多个高可信候选且五行不同，直接失败，不写成功。
- 同站同一期重复出现相同五行也必须确认属于同一权威数据块；不能仅因值相同就自动通过。
- 五行结果必须严格为金行、木行、水行、火行、土行之一。
- 期数正确但五行来源不确定、边界不唯一或关键词不匹配时必须失败。
- 站点修复验证只有在用户明确给出多个允许期数时，才可用其中任意一期验证；这不能改变正式手动单期必须匹配指定期数的规则。

## 11. 新增站点与重复检测

- 用户只提供站点但未说“开始”时，只记录名称、URL 和方向，不抓取、不判重、不写入。
- 开始后第一步检查同名目录；同名直接拒收。
- 继续检查同 URL、同 topic、同文章/记录 ID 和同关键字段；发现重复或疑似重复先报告。
- 候选站必须真实抓到近10期有效数据。抓取失败、SSL/TLS失败、无有效期数或候选为空一律拒收。
- 不足10期或不连续时，已有几期就用几期，但每期都必须有真实来源证据。
- 新增测试基准期只能取 `recent_10_cache.json` 的最新期或上一期，不能从候选页面自行推断。
- 正式判重只信正式重复检测服务和 `recent_10_cache.json`，不能使用旧实时指纹或临时人工指纹。
- 两站按共同拥有的具体期号对齐，不能按列表位置比较。
- 连续1至2期相同：不处理。
- 连续3至5期相同：疑似重复，等待人工确认。
- 连续6期或以上相同：重复拒收。
- “全部添加”不能覆盖同名、同 URL/topic/记录 ID、抓取失败、空历史或正式重复拒收等硬拦截。

## 12. 缓存事务与历史审查

- `recent_10_cache.json` 只保存正式站点最近10期成功结果，并包含 `schema_version`。
- 缓存条目按稳定 `site_id` 标识，保存期号、五行、抓取时间、来源摘要和 `rule_version`。
- 缓存写入必须依次执行：读取、结构校验、获得文件锁、冲突检查、写临时文件、重新读取验证、原子替换。
- 缓存读取损坏时必须停止，不能用空缓存覆盖。
- 同站同一期已有值和新值冲突时停止写入并报告，不能自动采用新值。
- 单期正式成功后才可更新缓存；失败站点不得更新。
- 多期模式、失败站独立验证、重复检测和迁移对照默认只读，不更新正式缓存。
- 缓存模式或 schema 升级必须通过独立迁移器；迁移前生成备份和差异报告，得到用户确认后才能替换正式缓存。
- 没有历史页面快照时，冲突只能标记为“历史解析错误或源站内容变更待确认”，不能无证据归因。

## 13. 单期、多期、判重和失败验证

- 单期入口调用统一 `ScrapeService`，按指定期号抓取并在成功后更新缓存。
- 多期入口对用户给出的多个期数逐期调用同一服务；某目录任意一期成功即通过，后续期数不再抓该目录。
- 多期模式不更新缓存，仍生成每期独立成功/失败 TXT，并额外生成全部期数失败汇总。
- 判重服务优先使用缓存中的近10期，只有明确调试模式才允许实时抓取；调试结果不能作为新增判重依据。
- 失败站验证使用正式服务的只读诊断模式，显示期号、五行、方向、锚点、关键词、边界、冲突和具体原因。
- 失败验证不得 monkey patch 正式解析函数，也不得维护只在验证脚本中生效的第二套解析器。
- 只有独立验证、正式单站和正式 BAT 都通过，修复才能标记完成。

## 14. 输出、进度和排行榜

- 成功 TXT 默认输出到 `C:\Users\Administrator\Desktop\每天工具\数据系列\七类数据统一归纳`。
- 失败 TXT 默认输出到 `C:\Users\Administrator\Desktop\每天工具\数据系列\七类数据统一归纳失败`。
- 失败 TXT 每个站点之间保留一个空行。
- 失败记录必须包含 URL、名称、方向、失败代码和具体中文原因，不能只写“失败”。
- 终端实时显示：`[进度 当前/总数 百分比% 成功 N 失败 N 用时 Ns] 当前: 目录名`。
- 成功 TXT 保留慢站耗时，并按累计耗时由高到低输出 Top 10。
- `澳门第二四不像`、`信封论坛`、`天青润薮`等用户明确排除项单独列出，不进入统计排行榜。
- 报告层只读取 `ScrapeResult`，不能通过重新解析成功字符串改变最终状态。

## 15. 测试与证据分层

必须保留以下测试层：

1. 单元测试：文本规范化、专属解析、方向窗口、冲突、缓存事务和错误分类。
2. 真实夹具契约测试：使用真实 HTML、API JSON、浏览器可见文本或 OCR 原始数据，包含正确记录和诱饵记录。
3. 流水线集成测试：验证获取、权威边界、解析、校验和结果对象完整贯通。
4. 入口兼容测试：验证单期、多期、判重、失败验证和现有 BAT 参数不变。
5. 架构测试：禁止跨层反向依赖、第二套解析器、直接修改全局函数和绕过统一服务。
6. 真实在线验收：覆盖 HTTP、浏览器、OCR、SPA、点击入口、长页面、top、bottom、admin 和 manager 站点。

新增或修复站点至少包含：正确目标、错误期号、同期冲突、其他文章诱饵、跨行内容、导航边界和方向窗口测试。

在线验证必须先使用独立输出和缓存副本。正式验收前不得覆盖生产成功/失败 TXT 或正式缓存。

## 16. 分阶段迁移与门禁

迁移必须按以下顺序进行，每阶段单独验证并标记：

- [x] M0：最终长期架构和操作规则写入本 `AGENTS.md`。
- [x] M1：冻结旧版测试、BAT 参数、输出格式、站点清单和真实抓取基线。
- [x] M2：建立 `domain`、`config`、`storage` 和 `reporting`，行为保持不变。
- [x] M3：迁移 HTTP、浏览器、API、SPA 和 OCR 来源适配器。
- [x] M4：建立唯一站点注册表，按页面结构迁移所有专属解析器。
- [x] M5：建立统一权威边界、方向、期号、标准五行和冲突校验引擎。
- [x] M6：建立唯一 `ScrapeService` 和批量执行服务。
- [x] M7：单期、多期、判重和失败验证全部迁移到统一服务。
- [x] M8：使用独立缓存和输出目录完成新旧版本逐站双跑对照。
- [x] M9：用户指定期数的正式全站 BAT 验收通过。
- [ ] M10：得到用户确认后切换正式入口，并清理修复版2中不再使用的迁移代码。

任何阶段失败时，只修复当前阶段，不得跳到下一阶段，也不得通过删除测试、放宽断言或修改基线掩盖差异。

## 17. 兼容策略与旧代码清理

- 迁移期间保留 `wuxing_crawler.py`、`wuxing_multi_period.py`、`wuxing_duplicate_flexible.py` 和 `failed_site_validator.py` 的原文件名与参数。
- 兼容入口最终只负责参数转换和调用新服务，目标是不超过约100行且不包含抓取、解析或缓存业务逻辑。
- BAT 文件名、输入方式、成功/失败 TXT 路径和用户工作流在切换前保持不变。
- 旧版和修复版2必须使用不同运行目录；迁移测试不得写入旧版代码目录。
- 只有所有调用方已迁移、架构测试证明无引用、正式验收通过并得到用户确认后，才能删除修复版2中的旧实现。
- 原 `杀五行-修复版` 不属于清理范围，始终保持不动，除非用户另行明确授权。

## 18. 完成定义

单个站点新增或修复只有同时满足以下条件才算完成：

- 真实访问目标页面或接口并保存结构证据。
- 专属插件、稳定 `rule_id` 和真实夹具已经建立。
- 指定期号、五行、方向、锚点、关键词、正文边界和冲突检查全部通过。
- 独立失败验证通过。
- 正式单站入口通过。
- 完整相关测试通过。
- 正式 BAT 验收通过。
- 缓存只写入经过验证的成功结果。

整个解耦项目只有同时满足以下条件才能把“解耦实施状态”改为“完成”：

- M1 至 M10 全部完成并有验证记录。
- 所有正式站点都有唯一 `site_id`、`rule_id`、专属规则和契约测试。
- 单期、多期、判重和失败验证调用同一正式服务。
- 不存在运行时 monkey patch、第二套解析逻辑或跨层反向依赖。
- 新旧版本逐站结果已经对照，所有差异均有证据和用户确认。
- `py -3` 下全部自动测试和正式全站 BAT 通过。
- 缓存事务、失败不写缓存和同期间冲突拦截已经真实验证。
- 用户明确确认正式切换和清理范围。

完成标记必须对应真实验收结果。不得因为文件已经拆分、代码已经写完或部分测试通过就提前标记完成。

## 19. 禁止事项

- 禁止一次性重写全部抓取逻辑后直接替换正式版本。
- 禁止为了减少代码量把专属解析退回通用正则。
- 禁止把复杂站点全部改成 JSON 正则配置而失去代码级边界校验。
- 禁止解析器访问网络、缓存或输出文件。
- 禁止抓取层判断最终五行。
- 禁止报告层重新决定成功或失败。
- 禁止通过缓存补齐手动指定期数的实时抓取结果。
- 禁止跨文章、跨文档、跨 iframe 或跨记录拼接标题和正文。
- 禁止以速度优化为理由减少真实验证、冲突检查或浏览器隔离。
- 禁止测试失败时删除测试、修改预期值或使用其他期数凑通过。
- 禁止缓存读取失败后写入空结构。
- 禁止在没有用户确认时修正历史缓存、切换正式 BAT 或删除旧代码。

## 20. 文件与编辑纪律

- 修改前读取本 `AGENTS.md`、相关代码、配置和测试。
- 手工编辑使用 `apply_patch`；结构化 JSON 使用安全解析、校验、锁和原子写入。
- 保留用户已有改动，不得使用破坏性 Git 命令或撤销无关变化。
- 每次改动都必须能对应到明确需求、测试和验收结果。
- 新增公开接口使用完整类型标注和清晰命名；复杂逻辑只注释“为什么”，不注释显而易见的操作。
- 优先简单、清晰和可验证的实现，不增加当前阶段不需要的框架和扩展机制。
- 每个迁移阶段完成后更新第16节复选框，并记录测试命令、真实抓取结果、缓存差异和剩余风险。

## 21. 阶段验收记录

- M3-M7：`py -3 -m unittest discover -q` 共 275 个测试通过；`ruff check wuxing cli tools tests` 通过；`py -3 -m compileall -q wuxing cli tools tests` 通过。
- M5/M6：真实缓存写入冲突测试证明旧值不被覆盖；同期冲突、重复五行、方向窗口、文章/用户边界均由统一校验服务处理。
- M8：已完成 211 期全站新旧只读双跑。分 13 批覆盖 153/153 个目录，无重复、无漏项；113 个结果一致，40 个结果差异，完整证据见 `migration/dual-run-full-report.json` 和 `migration/dual-run-batches/`。双跑使用独立缓存/输出且未把差异自动写入正式数据。
- M9：`杀五行_修复版2-单期.bat 211 --timeout 8 --workers 8 --config migration\sites.v2.preview.json --cache migration\formal-final-cache.json` 退出码 0，生成正式成功/失败 TXT；153 个目录中 104 个成功、49 个失败，失败分类为 `TARGET_NOT_FOUND` 42、`POSITION_WINDOW` 3、`ENTRY_NOT_FOUND` 2、`KEYWORD_MISMATCH` 1、`FETCH_TIMEOUT` 1，无缓存错误和未分类失败。成功结果写入独立验收缓存 104 条，详细报告见 `migration/formal-211-report.json`。
- 真实入口校验：八级心动精确点击后实际进入来者不笑正文，因缺少八级心动专属锚点被拒绝；男儿本色专属入口未形成唯一匹配，均未写入缓存。
- 本轮审核残留修复：补齐 OCR JavaScript 闭合语法并用 Node 真实编译；权威选择器禁止把独立 HTML/脚本/iframe 文档拼接为逻辑正文，并增加跨文档回归测试。
- 当前剩余：40 个新旧差异需要逐项证据审核；M10 用户确认后的正式切换和修复版2旧迁移代码清理。历史缓存修正仍未执行。
